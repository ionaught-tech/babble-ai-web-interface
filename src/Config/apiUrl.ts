const apiSever = "api.babble-ai.com";
const apiUrl =`https://${apiSever}/api/v1`;

export const socketUrl = `wss://${apiSever}`;

export const assetsUrl =
  "https://s3.ap-south-1.amazonaws.com/assets.babble-ai.com";
export default apiUrl;
